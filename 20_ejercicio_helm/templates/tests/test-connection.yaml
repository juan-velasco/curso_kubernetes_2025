apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "20_ejercicio_helm.fullname" . }}-test-connection"
  labels:
    {{- include "20_ejercicio_helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "20_ejercicio_helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
